description: Internal Server Error
content:
  application/json:
    example:
      {
        "message":"string",
        "ip":"ip"
      }
    schema:
      $ref: '../notes.yaml#/components/schemas/Error'
  application/xml:
    example: |
      <error>
        <message>string</item>
        <ip>string</item>
      </error>
    schema:
      $ref: '../notes.yaml#/components/schemas/Error'